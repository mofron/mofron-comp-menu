/**
 * @file   menu.js
 * @author simpart
 */

mofron.comp.Menu = class extends mofron.comp.Base {
    
    addElem(elm, cbf, cbp) {
        try {
        } catch (e) {
            conole.error(e.stack);
            throw e;
        }
    }
}
